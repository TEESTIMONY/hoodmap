import { Hash } from "lucide-react";
import { ComingSoon } from "@/components/layout/ComingSoon";

export default function ChannelsPage() {
  return (
    <ComingSoon
      icon={Hash}
      title="Channels are next"
      description="Topic-based communities with their own token-gating rules, live threads, and reputation-weighted moderation. In design now."
    />
  );
}
